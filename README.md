# EDA-App-Insights-Analysis
This dataset contains different mobile applications from the Google Play Store. We cleaned the data by removing missing values, converting data types, fixing text formats (like Installs, Price, and Size), removing duplicates, and converting the date column into proper datetime format
